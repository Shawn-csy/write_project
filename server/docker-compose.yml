version: '3.8'

services:
  api:
    build: .
    ports:
      - "1091:1091"
    volumes:
      - ./data:/data
      - .:/app
    restart: unless-stopped
    environment:
      - PORT=1091
      - DB_PATH=/data/scripts.db
