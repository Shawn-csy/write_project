// Re-export from shared package — single source of truth for the renderer.
export { RenderBlockRenderer } from "@write/script-reader-renderer";
export type { RenderBlockRendererProps, MarkerConfigLike } from "@write/script-reader-renderer";
